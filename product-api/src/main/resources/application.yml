spring:
  application:
    name: product-management
  mvc:
    format:
      enums:
        ignore-case: true
  profiles:
    active:
      - dev

ai:
  recipe:
    api:
      url: http://localhost:8000/api

api:
  security:
    token:
      secret: gise-terces
      expiration: 86400000
